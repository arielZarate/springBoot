# Application configuration for the Order Service
server:
  port: 8080
  servlet:
    context-path: /api
# db  configuration
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/orders
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 30000
      pool-name: HikariPool
      max-lifetime: 600000
      connection-timeout: 30000
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
# Swagger / openApi   configuration
springdoc:
  api-docs:
    path: /docs
  swagger-ui:
    path: /swagger

# Logging configuration
#logging:
#  level:
#    root: INFO
#    com.arielzarate: DEBUG
#  pattern:
#    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
#  file:
#    name: logs/orderservice.log



# Actuator configuration
#management:
#  endpoints:
#    web:
#      exposure:
#        include: health,info,metrics,httptrace
#  endpoint:s:
#    health:
#      show-details: always
#  metrics:
#    enable: all: true
# Security configuration
#security:
#  user:
#    name: admin
#    password: admin123
#  oauth2:
#    resourceserver:
#      jwt:
#        issuer-uri: https://auth.example.com/

